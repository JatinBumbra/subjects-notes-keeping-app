import { css } from 'styled-components';

const wrapper = css`
  padding: 16px 5%;
  margin-top: 8px;
`;
const title = css`
  color: black;
  font-size: 24px;
  font-weight: bold;
`;

const headerStyles = Object.freeze({
  wrapper,
  title,
});
export default headerStyles;
